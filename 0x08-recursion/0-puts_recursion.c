#include "main.h"
/**
  *_puts_recursion - puts function ();
  *@s: input
  *Return: Always o
  */
void _puts_recursion(char *s)
{
	if (*s)
	{
		void _putchar(*s);
		_puts_recursion(s +);
	}

	else
		_putchar('\n');
}
